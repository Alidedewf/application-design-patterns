# 05 — Singleton

| Файл | Что демонстрирует |
|---|---|
| `lab/logger/main.go` | Потокобезопасный логгер: единственный экземпляр через `sync.Once`, запись в файл защищена `sync.Mutex`. Демонстрация — 3 горутины пишут в лог одновременно на разных уровнях (INFO/WARNING/ERROR) |
| `lab/builder/main.go` | Заготовка под Builder — не реализована (только объявление пакета) |

```bash
go run "./05 module/lab/logger/main.go"
```
